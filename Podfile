target 'iosApp' do
    use_frameworks!
    platform :ios, '14.1'
    pod 'shared', :path => '../SepparatedApp(Android&Shared)/shared'
end

target 'watchApp' do
    use_frameworks!
    platform :watchos, '7.4'
    pod 'shared', :path => '../SepparatedApp(Android&Shared)/shared'
end
